@page
@model IndexModel

@section Styles {

}

<div id="get-index">
    <br />

    <table id="product" class="table table-bordered table-hover">
        <thead>
            <tr class="">
                <td>Name</td>
                <td>Price</td>
                <td>Quantity</td>
            </tr>
        </thead>

        <tbody>
            @foreach (var product in Model.ProductViewModel.Product)
            {
                <tr style="height: 60px;">
                    <td>@product.Name</td>
                    <td>@product.Price</td>
                    <td>@product.Quantity</td>
                </tr>
            })
        </tbody>
    </table>
</div>
